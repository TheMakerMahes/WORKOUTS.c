//enumerations or enum in c programming
#include<stdio.h>
 enum mahes{no,yes=20};

 int main()
 {

     enum maddy {x=10,y=20};
     enum mahes a;
     enum maddy b;
     enum mr {d=10,f};
     enum mr c;
     a=no;
     b=y;
     c=f;
     printf("\nA : %d",a);
     printf("\nB : %d",b);
     printf("\nC : %d",c);

 }
